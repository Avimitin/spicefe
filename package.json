{
  "name": "spicefe",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run typecheck && esbuild src/app.tsx --bundle --format=esm --platform=browser --target=es2022 --define:process.env.NODE_ENV=\"'production'\" --minify --legal-comments=none --outfile=public/app.js",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.5"
  },
  "dependencies": {
    "jmuxer": "2.1.1",
    "qrcode-generator": "2.0.4",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  }
}
